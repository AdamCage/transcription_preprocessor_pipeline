# VAD service configuration (all values have sensible defaults)
# VAD_DEVICE=cuda:0
# VAD_MAX_CONCURRENCY=8
# VAD_EXECUTOR_WORKERS=1
# VAD_HOST=0.0.0.0
# VAD_PORT=8002
# VAD_LOG_LEVEL=info
# VAD_LOG_DIR=logs
# VAD_MODEL_PATH=
# VAD_MAX_AUDIO_SIZE_MB=200.0
# VAD_INFERENCE_TIMEOUT_SEC=120.0
